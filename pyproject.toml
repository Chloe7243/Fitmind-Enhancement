[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Fitmind"
authors = [{name = "Samuel Bolt", email = "up2206078@myport.ac.uk"}]
dynamic = ["version", "description"]
